# El frontend no necesita configuración para desarrollo: habla con la API por
# /api en el mismo origen, gracias al proxy del dev server de Vite.
#
# En producción, si la API se despliega en otro host, aquí iría su URL base y el
# cliente HTTP la antepondría a las rutas.
VITE_API_BASE_URL=
